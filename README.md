# primeiroMobile
Teste de aplicação mobile

# Comandos basicos de instalação
- sudo apt update
- sudo apt install -y nodejs npm
- npm install --global expo-cli
- npx create-expo-app ex01 --template blank

# Rodar o expo no porjeto escolhido
- npx expo start --tunnel


# Termos avulsos
- Broker?
